
// Feature flags for enabling/disabling features
export const featureFlags = {
  classCodeEnrollmentEnabled: false, // Disabled by default
  mockEnrollmentEnabled: true, // Enable mock enrollment for testing
};
